---
import { redirect } from 'astro:redirect';

export const prerender = true;

// Redirect to your PDF (served from /public)
redirect('/Clinton-Marrs-Portfolio_Manager-Brand-Studio-Webflow.pdf', 302);
---
